{
  "tasks": {
    // TODO: Add flags when globs are available: 
    //  --allow-read=Cargo.toml,dist/*, npm-assets/*
    //  --allow-write=dist/*
    "build": "deno run --allow-run --allow-read --allow-write scripts/build-npm.ts"
  }
}
